import { View } from "react-native";
import { AppText } from "@/components/ui/AppText";

export const DocumentsScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <AppText>Documents Screen</AppText>
    </View>
  );
};